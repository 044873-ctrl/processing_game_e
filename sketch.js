let player={x:200,y:560,r:14,speed:5};let bullets=[];let enemies=[];let particles=[];let stars=[];let score=0;let gameOver=false;function setup(){createCanvas(400,600);for(let i=0;i<30;i++){let s={x:random(0,width),y:random(0,height),speed:random(0.5,2),r:random(1,3)};stars.push(s);}noStroke();textAlign(LEFT,TOP);textSize(16);}function draw(){background(0);for(let i=0;i<stars.length;i++){let s=stars[i];fill(255);circle(s.x,s.y,s.r);s.y+=s.speed;if(s.y>height){s.y=0;s.x=random(0,width);}}if(!gameOver&&frameCount%60===0){let e={x:random(12,width-12),y:-12,r:12,vy:2};enemies.push(e);}if(keyIsDown(LEFT_ARROW)){player.x-=player.speed;}if(keyIsDown(RIGHT_ARROW)){player.x+=player.speed;}player.x=constrain(player.x,player.r,width-player.r);for(let i=bullets.length-1;i>=0;i--){let b=bullets[i];fill(50,200,255);circle(b.x,b.y,b.r*2);b.y-=b.vy;if(b.y<-b.r){bullets.splice(i,1);}}for(let i=enemies.length-1;i>=0;i--){let e=enemies[i];fill(255,50,50);circle(e.x,e.y,e.r*2);e.y+=e.vy;if(e.y>height+e.r){enemies.splice(i,1);continue;}if(!gameOver){if(dist(e.x,e.y,player.x,player.y)<=e.r+player.r){gameOver=true;}}for(let j=bullets.length-1;j>=0;j--){let b=bullets[j];if(dist(e.x,e.y,b.x,b.y)<=e.r+b.r){for(let p=0;p<5;p++){let angle=random(0,TWO_PI);let speed=random(1,4);let part={x:e.x,y:e.y,vx:cos(angle)*speed,vy:sin(angle)*speed,r:3,life:20};particles.push(part);}score++;enemies.splice(i,1);bullets.splice(j,1);break;}}}for(let i=particles.length-1;i>=0;i--){let p=particles[i];fill(255,200,0,map(p.life,0,20,0,255));circle(p.x,p.y,p.r*2);p.x+=p.vx;p.y+=p.vy;p.life--;if(p.life<=0){particles.splice(i,1);}}fill(0,150,255);circle(player.x,player.y,player.r*2);fill(255);text('Score: '+score,8,8);if(gameOver){textAlign(CENTER,CENTER);textSize(32);text('GAME OVER',width/2,height/2);textSize(16);textAlign(LEFT,TOP);}}function keyPressed(){if(!gameOver&&keyCode===32){let b={x:player.x,y:player.y-player.r-4,r:4,vy:8};bullets.push(b);}if(gameOver&&key==='r'){player.x=200;player.y=560;bullets=[];enemies=[];particles=[];score=0;gameOver=false;}}
