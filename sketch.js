function setup(){createCanvas(600,400);initGame()}function draw(){background(10,24,48);drawScore();if(gameOver){fill(255);textAlign(CENTER,CENTER);textSize(36);text('GAME OVER',width/2,height/2-20);drawRetryButton();return}handleInput();applyPhysics();handleCollisions();drawPlatforms();drawPlayer();drawStars()}let player;let platforms;let stars;let score;let gameOver=false;let groundHeight=20;let groundY;let jumpPower= -11;let gravity=0.6;let maxFall=12;let playerSpeed=3.5;let previousStanding=null;function initGame(){groundY=height-groundHeight;player={x:50,y:groundY-30,w:30,h:30,vy:0,canJump:true};platforms=[];stars=[];score=0;gameOver=false;previousStanding=null;let lowestY=groundY-80;let xs=[];for(let i=0;i<5;i++){let px=Math.floor(random(20,width-120));while(xs.includes(px)){px=Math.floor(random(20,width-120))}xs.push(px);let py; if(i===0){py=lowestY}else{py=Math.floor(random(groundY-220,groundY-120))}platforms.push({x:px,y:py,w:100,h:10})}platforms.sort((a,b)=>b.y-a.y);if(platforms.length>0){let lowest=platforms[platforms.length-1];if(lowest.y>groundY-90){lowest.y=groundY-80}}for(let i=0;i<platforms.length;i++){let p=platforms[i];let sx=p.x+Math.floor(p.w/2);let sy=p.y-10;stars.push({x:sx,y:sy,r:6})}for(let i=0;i<3;i++){let sx=Math.floor(random(20,width-20));let sy=Math.floor(random(50,groundY-150));stars.push({x:sx,y:sy,r:6})}}function drawScore(){fill(255);textSize(16);textAlign(LEFT,TOP);text('SCORE: '+score,10,10)}function handleInput(){if(keyIsDown(LEFT_ARROW)){player.x-=playerSpeed}if(keyIsDown(RIGHT_ARROW)){player.x+=playerSpeed}player.x=constrain(player.x,0,width-player.w)}function applyPhysics(){player.vy+=gravity;if(player.vy>maxFall){player.vy=maxFall}player.y+=player.vy}function handleCollisions(){let standingOn=null;let prevBottom=player.y-player.vy+player.h;for(let i=0;i<platforms.length;i++){let p=platforms[i];let playerBottom=player.y+player.h;let overlapX=(player.x+player.w>p.x)&&(player.x<p.x+p.w);if(player.vy>=0 && prevBottom<=p.y && playerBottom>=p.y && overlapX){player.y=p.y-player.h;player.vy=0;standingOn=i;player.canJump=true}}let playerBottom=player.y+player.h;if(playerBottom>=groundY){player.y=groundY-player.h;player.vy=0;standingOn='ground';player.canJump=true}if(previousStanding!==null && previousStanding!=='ground' && standingOn==='ground'){gameOver=true}previousStanding=standingOn;for(let i=stars.length-1;i>=0;i--){let s=stars[i];if(rectContainsPoint(player,s.x,s.y)){stars.splice(i,1);score+=10}}}function rectContainsPoint(r,pX,pY){return pX>r.x && pX<r.x+r.w && pY>r.y && pY<r.y+r.h}function drawPlatforms(){fill(120,90,60);for(let i=0;i<platforms.length;i++){let p=platforms[i];rect(p.x,p.y,p.w,p.h)}fill(80,50,40);rect(0,groundY,width,groundHeight)}function drawPlayer(){fill(200,40,40);rect(player.x,player.y,player.w,player.h)}function drawStars(){fill(250,210,50);noStroke();for(let i=0;i<stars.length;i++){let s=stars[i];ellipse(s.x,s.y,s.r*2,s.r*2)}stroke(0)}function keyPressed(){if(key===' '||keyCode===32){if(!gameOver && player.canJump){player.vy=jumpPower;player.canJump=false;previousStanding=null}}if(key==='r' || key==='R'){initGame()}}function mousePressed(){if(gameOver){let bx=width/2-40;let by=height/2+10;let bw=80;let bh=30;if(mouseX>=bx && mouseX<=bx+bw && mouseY>=by && mouseY<=by+bh){initGame()}}}function drawRetryButton(){let bx=width/2-40;let by=height/2+10;let bw=80;let bh=30;fill(50,150,200);rect(bx,by,bw,bh,5);fill(255);textSize(14);textAlign(CENTER,CENTER);text('RETRY',bx+bw/2,by+bh/2)}
